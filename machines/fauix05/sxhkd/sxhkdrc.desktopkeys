super + {_,shift + }Tab
    bspc monitor --focus {next,prev}

super + {_,shift + }bracket{left,right}
    bspc {monitor --focus,window --to-monitor} {left,right}

super + {_,shift + }{1-9,0}
    bspc {desktop --focus,window --to-desktop} {web,code,cod3,term,more,x,y,z,9,comm}
